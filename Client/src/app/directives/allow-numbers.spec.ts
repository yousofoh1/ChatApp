import { AllowNumbers } from './allow-numbers';

describe('AllowNumbers', () => {
  it('should create an instance', () => {
    const directive = new AllowNumbers();
    expect(directive).toBeTruthy();
  });
});
