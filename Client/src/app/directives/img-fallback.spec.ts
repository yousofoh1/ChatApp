import { ImgFallback } from './img-fallback';

describe('ImgFallback', () => {
  it('should create an instance', () => {
    const directive = new ImgFallback();
    expect(directive).toBeTruthy();
  });
});
